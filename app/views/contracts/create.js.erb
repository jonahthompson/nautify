if (window.contract_section[0] === 'j') {
	$('#' + window.contract_section).find('.contracts').html(`<%= j render 'jobs/job_contracts', job: @contract.job %>`);
} else {
$('#' + window.contract_section).find('.contracts').html(`<%= j render 'boats/boat_contracts', boat: @contract.boat %>`);
}