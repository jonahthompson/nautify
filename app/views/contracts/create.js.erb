$('#' + window.job_section).find('.contract').append(`
	<%= @boat.name %> <%= link_to contract_path, method: :delete, remote: true %>
	`);
$('.new_boat_job').trigger('reset');


$('#' + window.boat_section).find('.boat_jobs').append(`
	<%= @job.name %> <%= link_to image_tag("delete.png", class: "format_small"), destroy_boat_job_path(boat_id: @boat.id, job_id: @job.id),  method: :delete, remote: true %>
	`);
$('.new_job_boat').trigger('reset');